const path = require('path');

module.exports = {
  config: path.resolve('config', 'config.js'),          // DB config from .env
  modelsPath: path.resolve('src', 'models'),            // Sequelize models
  migrationsPath: path.resolve('migrations'),           // DB migrations
  seedersPath: path.resolve('seeders'),                 // Seed files
};
