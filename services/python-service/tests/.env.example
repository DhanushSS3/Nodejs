# Python Service Configuration

# Redis Configuration
REDIS_PASSWORD=admin@livefxhub@123
REDIS_HOSTS=127.0.0.1:7001,127.0.0.1:7002,127.0.0.1:7003

# WebSocket Listener Configuration
# Set to "true" for binary/protobuf listener (recommended for production)
# Set to "false" for JSON listener (fallback mode)
USE_BINARY_LISTENER=true

# Environment Mode
ENVIRONMENT=production

# Service URLs (for development)
PYTHON_SERVICE_URL=http://localhost:8000
NODEJS_SERVICE_URL=http://localhost:3000
